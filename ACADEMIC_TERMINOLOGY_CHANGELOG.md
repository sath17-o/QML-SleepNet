# Documentation terminology revision

This revision standardizes evaluator-facing terminology across the repository without changing model weights, preprocessing logic, metrics, artifact hashes or scientific conclusions.

Academic documentation now uses the terms **Stage06 hybrid model**, **reference QML system**, **final integrated system**, **methodology replay**, and **versioned artifacts**. Historical artifact paths and machine-readable identifiers are retained only where required for provenance and executable reproducibility.
